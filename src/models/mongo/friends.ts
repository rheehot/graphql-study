// const mongoose = require('mongoose');
// export const Dog: any = mongoose.model("Dog", { name: String, input: String!})
const mongoose = require('mongoose');
export const Friend: any = mongoose.model("Friend", { name: String, age: String, input: String})